apiVersion: v1
kind: Service
metadata:
  name: mikrotik-cert-controller
  labels:
    app.kubernetes.io/name: mikrotik-cert-controller
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mikrotik-cert-controller
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
      protocol: TCP
